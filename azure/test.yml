steps:
- task: VSTest@2
  displayName: 'Test Assemblies'
  inputs:
    testAssemblyVer2: |
     **\$(BuildConfiguration)\x64\*test*.dll
     !**\obj\**
    codeCoverageEnabled: true
    configuration: '$(BuildConfiguration)'